# SPEC — <task name>

> Produced by the **Spec** stage. Restate the assignment as a crisp, checkable spec. No code yet.

## Objective
<one or two sentences: what done looks like>

## Inputs (what this gear consumes)
- <handoff / artifact paths it was handed>

## Acceptance criteria (numbered, each INDEPENDENTLY checkable)
1. <criterion — phrased so a validator can pass/fail it with evidence>
2. ...

## Out of scope
- <explicitly not doing>

## Boundaries
- write only inside the assigned workdir; touch no other repos; no client/Banafah work
