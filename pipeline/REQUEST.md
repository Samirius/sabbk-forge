# Work Request (pipeline input for Pi PM)

**objective:** Get a new jig authored for the workshop — a script that fails when any tracked `.md`/`.sh`
file under a directory has trailing whitespace (the same internal forge task as `spike/TASK.md`).

**context:** internal tooling improvement; no client involvement.
**desired_outcome:** a reviewed, validated `no-trailing-whitespace.sh` ready for a human to PR into `sabbk-workshop/jigs/`.
**constraints:** internal only; coding agent stays inside its workdir; no other repos touched.

Pi PM: turn this into a compliant assignment handoff to `pi-coding-spike`, then stop at the dispatch checkpoint.
